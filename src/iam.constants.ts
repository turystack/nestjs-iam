/** DI token for the IAM module options (secret, permissions, profileResolver). */
export const IAM_OPTIONS = Symbol('IAM_OPTIONS')

/** Metadata key used by the @ACL decorator. */
export const ACL_KEY = 'iam:acl'
